---
description: Show detailed GLM Coding Plan quota and usage information.
disable-model-invocation: true
---

# GLM Plan Details

Run the plugin details command:

```bash
glm-statusline.js --plan-details
```

Report the command output as-is. If the command fails, report the error output and suggest checking `ANTHROPIC_AUTH_TOKEN` and `ANTHROPIC_BASE_URL` in Claude Code settings.
