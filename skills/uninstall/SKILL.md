---
description: Remove the GLM status line from Claude Code user settings.
---

# Uninstall GLM StatusLine

Run the plugin uninstaller from the plugin `bin` directory:

```bash
glm-statusline-install.js uninstall
```

Then tell the user whether the status line setting was removed. If another status line is active, do not force removal unless the user explicitly asks for it.
